import 'package:flutter/material.dart';

void main() {
  runApp(const AppleMapsExample());
}

class AppleMapsExample extends StatelessWidget {
  const AppleMapsExample({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
